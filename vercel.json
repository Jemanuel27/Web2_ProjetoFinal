
  {
    "version": 2,
    "builds": [
        {
            "src": "./app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/",
            "dest": "/public/cadastro.html"
        };
    {
      "src": "/api/(.*)",
      "dest": "/api/$1" // Se você tiver rotas de API
    }
  ]
}
